version: 2
updates:
  - package-ecosystem: github-actions
    directories: [/, /workflow-templates]
    schedule:
      interval: weekly
    groups:
      actions-minor-patch:
        group-by: dependency-name
        patterns: ['*']
        update-types: [minor, patch]
